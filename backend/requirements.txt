asgiref==3.12.1
Django==6.1
django-cors-headers==4.9.0
djangorestframework==3.18.0
pillow==12.3.0
python-dotenv==1.2.2
sqlparse==0.6.0
tzdata==2026.3
gunicorn==23.0.0

# Facturación electrónica ARCA/AFIP (WSAA + WSFEv1).
# El paquete PyAfipWs de PyPI (2.7.1874) no instala en Python 3.12+: su .tar.gz trae
# un symlink que la extracción segura de tarfile rechaza. Se toma el código de GitHub,
# que sí instala limpio, fijado a un commit.
# Va por el archivo .tar.gz y NO por `git+https://` a propósito: el contenedor de la
# API corre `pip install -r requirements.txt` en cada arranque sobre python:3.13-slim,
# que no trae git — con la forma `git+` el arranque falla y la API no levanta.
pyafipws @ https://github.com/reingart/pyafipws/archive/d595b072110accec9dae1ddb58165ab847b8520a.tar.gz
# pysimplesoap (dependencia de pyafipws) todavía usa `distutils`, que Python 3.12+ sacó
# de la stdlib — setuptools trae el shim que lo vuelve a exponer.
setuptools

